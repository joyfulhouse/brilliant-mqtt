# Virtual Control feasibility gates

This runbook determines whether one disposable, officially provisioned
Brilliant Virtual Control can safely host one Home Assistant-backed native
light and make that light selectable from a physical Control slider. A passing
experiment authorizes a later production design; it is not a deployment.

## Non-negotiable boundaries

- Run VC0 through VC5 in order and stop on the first `blocked` or `fail` result.
- Never provision from an account JWT, guessed GraphQL mutation, invented
  device identity, or direct cloud database write.
- Never print or copy a token, private key, certificate, PKCS#12 value,
  bootstrap blob, password, home ID, or unredacted device ID into repository
  output.
- Identity material stays on Office under `/data/brilliant-vc/identity` with a
  `0700` directory and `0600` files.
- A live process is bounded and supervised. Abort on physical-control lag,
  cloud-peer disconnect, bus peer timeout, sustained CPU above 15%, RSS above
  100 MiB, or uncertain cleanup.
- Do not execute Brilliant scenes as part of these gates. Control tests use
  only the disposable HA light and one separately approved physical slider.

## VC0 — prior-state audit

1. Collect Office credential-path metadata using
   `python -m tools.brilliant_vc.audit --collect-stats <ignored-path>` on the
   panel. This operation calls `lstat` only.
2. Collect a redacted bus/home snapshot containing firmware, a hash of the home
   ID, physical Control count, and first-four/last-four identifiers only for
   DeviceType 6 identities.
3. Independently inspect the device inventory in the official Brilliant app.
   Record sanitized counts and redacted identity matches, not screenshots with
   personal information.
4. Run the audit with `--panel`, `--snapshot-json`, `--stat-json`, and
   `--output`. VC0 passes only when app and bus inventories agree, no
   unexplained Virtual Control exists, no July 9 VC record exists, and every
   retained credential-shaped path is root-only with an explicit reason.

## VC1 — official bootstrap-token provenance

Use the official Brilliant app on an operator-controlled handset and account
session. Navigate only the app's supported add-device/add-Control workflow and
observe the request generated by that workflow using ordinary OS/app diagnostic
facilities or an operator-controlled network capture.

Do not enumerate mutation names, fuzz request fields, or replay unrelated
production calls. The allowed outcomes are:

- The app has no Virtual Control path or produces no provisioning-scoped token:
  record VC1 `blocked` and stop.
- TLS pinning prevents normal observation: record VC1 `blocked` unless the
  operator separately authorizes app instrumentation.
- The official flow yields a provisioning token: retain it only in a root-owned
  `0600` file on Office and run:

  ```text
  python -m tools.brilliant_vc.token_check \
    --token-file <root-only-path> \
    --report <ignored-sanitized-report>
  ```

The report is explicitly claims-only. VC1 passes only when its time claims are
valid and its exact allowlist contains
`/provisioning/virtual-control-self-bootstrap`. The shipped Brilliant client and
server remain responsible for cryptographic authentication at VC2.

## VC2 approval stop

Before the single account-visible provisioning write, stop and obtain a fresh
approval that names the target home/property, Office as the identity host, one
disposable Virtual Control, root-only identity storage, the cloud account
change, and mandatory official removal. An earlier general approval is not the
short-lived approval file used by the provisioner.

## VC5 physical-slider acceptance

Tile rendering and physical-slider assignability are separate gates. After one
VC-hosted `LIGHT` renders:

1. Confirm it appears in the native physical-slider target picker. Absence is a
   blocked result; never bypass the picker with a hand-written `slider_config`.
2. Snapshot the selected Office slider's complete binding and wired/default
   behavior, then obtain approval naming that slider.
3. Bind through the native UI. Verify one tap and one dim gesture each reach HA
   exactly once and that HA state feeds back to both panels.
4. Verify all physical loads remain responsive through process, HA, MQTT, and
   panel restart checks.
5. Restore the exact original binding through the native UI. Two later
   snapshots must contain no VC/pilot reference before the hosted light and
   disposable Virtual Control are removed through the official workflow.
