# ─── SECRETS — never commit ──────────────────────────────────────────────
# Operator credentials + hardware details for the implementation agent.
# This file is intentionally gitignored and lives ONLY on the dev machine.
CREDENTIALS.local.md
*.local.md
.env
.env.*
secrets/
*.key
*.pem
id_ed25519*

# ─── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Build artifact bundled into the HACS zip (scripts/build_payload.sh)
custom_components/brilliant_mqtt/agent_payload/

# Voice payload build artifacts (scripts/build_voice_payload.sh)
custom_components/brilliant_mqtt/voice_payload/

# ─── Tooling caches ──────────────────────────────────────────────────────
.worktrees/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
uv.lock.bak

# ─── Editor / OS ───────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# Internal working artifacts (operator-local; not part of the public tree)
docs/claude/

# Proprietary Brilliant panel corpus (raw /data and /var captures, extracted
# binaries, decompiler projects, and analysis scratch space). Only sanitized
# manifests and documentation belong in git.
artifacts/brilliant-panel/
